allowBuilds:
  # Native SQLite binding — required, this is the database driver.
  better-sqlite3: true
  # Build tooling for tsx/esbuild.
  esbuild: true
  # Optional ws speedups; the pure-JS fallbacks are fine.
  bigint-buffer: false
  bufferutil: false
  utf-8-validate: false
